/*
import { describe, it, expect } from 'vitest';
import { mount } from '@vue/test-utils';
import Example from '../Example.vue';

describe('Example', () => {
    it('renders properly', () => {
        const wrapper = mount(Example, { props: { msg: 'Hello Vitest' } });
        expect(wrapper.text()).toContain('Hello Vitest');
    });
});
*/
